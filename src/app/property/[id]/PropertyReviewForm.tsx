export default function PropertyReviewForm({ id }: { id: string }) {
  return (
    <div className="p-4">
      <h2 className="text-xl font-semibold">매물 상세 정보</h2>
      {/* 추후 fetch로 불러오거나 props로 넘기기 */}
    </div>
  );
}
